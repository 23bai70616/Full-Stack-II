import { Container, Typography } from "@mui/material";

function Home() {
  return (
    <Container sx={{ mt: 5 }}>
      <Typography variant="h4">Home Page</Typography>
      <Typography>Experiment 3 Home Page</Typography>
    </Container>
  );
}

export default Home;
