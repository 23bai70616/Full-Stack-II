import { Container, Typography } from "@mui/material";

function About() {
  return (
    <Container sx={{ mt: 5 }}>
      <Typography variant="h4">About Page</Typography>
      <Typography>React Router Navigation</Typography>
    </Container>
  );
}

export default About;
